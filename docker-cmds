clone frontend repo to server
inside repo create folder: mkdir server
cp config to server: cp -r config ./server
Give rights to folder: sudo chown administrator server/
build files locally and filezilla them into server folder


docker build -f Dockerfile-slim -t fms-frontend-image . && \
docker run -d --name fms-frontend --restart always -p 8006:80 fms-frontend-image
docker update --restart always fms-frontend
docker rm -f fms-frontend

-- update --
docker build -f Dockerfile-slim -t fms-frontend-image . && docker rm -f fms-frontend && docker run -d --name fms-frontend --restart always -p 8006:80 fms-frontend-image